version: '3.8'
services:
  chat-ui:
    build: .
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - PORT=8080
      - N8N_WEBHOOK_URL=http://host.docker.internal:5678/webhook/chat
      - ALLOWED_ORIGIN=*
