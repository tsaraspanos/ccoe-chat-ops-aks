apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ccoe-chat-ops-ingress
  namespace: ccoe-chat-ops
  annotations:
    # For NGINX Ingress Controller
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    # SSE support - disable buffering
    nginx.ingress.kubernetes.io/proxy-buffering: "off"
    # For cert-manager (optional - comment out if not using)
    # cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  # Uncomment and configure TLS for HTTPS
  # tls:
  #   - hosts:
  #       - chat.yourdomain.com
  #     secretName: ccoe-chat-ops-tls
  rules:
    - host: chat.yourdomain.com  # UPDATE: Your domain
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ccoe-chat-ops
                port:
                  number: 80
